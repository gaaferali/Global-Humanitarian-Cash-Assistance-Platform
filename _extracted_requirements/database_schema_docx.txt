
Simplified MVP Database Schema
Purpose: viable schema for a multi-tenant cash assistance / beneficiary payment system.

Scope: registration, program management, eligibility approval, payment execution, complaints, budgeting, and audit.

Tables: 12 core tables.

1. Relationship diagram







2. Relationships
Parent
Child
FK
Cardinality
tenant
user
tenant_id
1:N
tenant
program
tenant_id
1:N
tenant
household
tenant_id
1:N
tenant
audit_event
tenant_id
1:N
user
program
created_by
1:N
user
household
created_by
1:N
user
beneficiary
created_by
1:N
user
payment_instruction
created_by
1:N
program
household
program_id
1:N
program
enrollment
program_id
1:N
program
payment_channel_config
program_id
1:N
program
budget
program_id
1:1
household
beneficiary
household_id
1:N
beneficiary
enrollment
beneficiary_id
1:N
enrollment
payment_instruction
enrollment_id
1:N
payment_channel_config
payment_instruction
channel_config_id
1:N
payment_instruction
payment_event
instruction_id
1:N
beneficiary
complaint
beneficiary_id
1:N
payment_instruction
complaint
instruction_id
1:N
budget
budget_line
budget_id
1:N
4. Tables and columns
tenant
Column
Type
Key
Constraints
tenant_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
name
TEXT

NOT NULL
tenant_type
TEXT

NOT NULL
default_currency
TEXT(3)

NOT NULL
is_active
BOOLEAN

NOT NULL, DEFAULT true
created_at
TIMESTAMPTZ

NOT NULL, DEFAULT now()
user
Column
Type
Key
Constraints
user_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
tenant_id
UUID
FK
NOT NULL, REFERENCES tenant(tenant_id)
email
TEXT
UNIQUE
NOT NULL
full_name
TEXT

NOT NULL
password_hash
TEXT

NOT NULL
role
TEXT

NOT NULL, CHECK IN (ADMIN, FIELD_OFFICER, FINANCE, REVIEWER)
is_active
BOOLEAN

NOT NULL, DEFAULT true
created_at
TIMESTAMPTZ

NOT NULL, DEFAULT now()
program
Column
Type
Key
Constraints
program_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
tenant_id
UUID
FK
NOT NULL, REFERENCES tenant(tenant_id)
name
TEXT

NOT NULL
currency
TEXT(3)

NOT NULL
transfer_amount
NUMERIC(18,2)

NOT NULL, CHECK > 0
payment_cycle
TEXT

NOT NULL, CHECK IN (ONE_TIME, MONTHLY, QUARTERLY)
status
TEXT

NOT NULL, DEFAULT DRAFT
start_date
DATE

NULL
end_date
DATE

NULL
created_by
UUID
FK
NOT NULL, REFERENCES user(user_id)
created_at
TIMESTAMPTZ

NOT NULL, DEFAULT now()
household
Column
Type
Key
Constraints
household_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
tenant_id
UUID
FK
NOT NULL, REFERENCES tenant(tenant_id)
program_id
UUID
FK
NOT NULL, REFERENCES program(program_id)
household_size
INT

NOT NULL, CHECK > 0
location
TEXT

NOT NULL
registration_date
DATE

NOT NULL
client_generated_id
TEXT

NULL, unique per tenant
created_by
UUID
FK
NOT NULL, REFERENCES user(user_id)
created_at
TIMESTAMPTZ

NOT NULL, DEFAULT now()
beneficiary
Column
Type
Key
Constraints
beneficiary_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
household_id
UUID
FK
NOT NULL, REFERENCES household(household_id)
full_name
TEXT

NOT NULL
gender
TEXT

NULL
date_of_birth
DATE

NULL
phone_last4
TEXT(4)

NULL
national_id_hash
TEXT

NULL
consent_given
BOOLEAN

NOT NULL, DEFAULT false
verification_status
TEXT

NOT NULL, DEFAULT PENDING
status
TEXT

NOT NULL, DEFAULT REGISTERED
created_by
UUID
FK
NOT NULL, REFERENCES user(user_id)
created_at
TIMESTAMPTZ

NOT NULL, DEFAULT now()
enrollment
Column
Type
Key
Constraints
enrollment_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
beneficiary_id
UUID
FK
NOT NULL, REFERENCES beneficiary(beneficiary_id)
program_id
UUID
FK
NOT NULL, REFERENCES program(program_id)
eligibility_status
TEXT

NOT NULL, DEFAULT PENDING
status
TEXT

NOT NULL, DEFAULT ENROLLED
enrolled_at
TIMESTAMPTZ

NOT NULL, DEFAULT now()
approved_by
UUID
FK
NULL, REFERENCES user(user_id)
approved_at
TIMESTAMPTZ

NULL
UNIQUE
(program_id, beneficiary_id)

one enrollment per program
payment_channel_config
Column
Type
Key
Constraints
channel_config_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
program_id
UUID
FK
NOT NULL, REFERENCES program(program_id)
channel_type
TEXT

NOT NULL, CHECK IN (BANK, MOBILE_MONEY, CASH, VOUCHER)
provider_name
TEXT

NOT NULL
currency
TEXT(3)

NOT NULL
is_active
BOOLEAN

NOT NULL, DEFAULT true
payment_instruction
Column
Type
Key
Constraints
instruction_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
enrollment_id
UUID
FK
NOT NULL, REFERENCES enrollment(enrollment_id)
beneficiary_id
UUID
FK
NOT NULL, REFERENCES beneficiary(beneficiary_id)
channel_config_id
UUID
FK
NOT NULL, REFERENCES payment_channel_config(channel_config_id)
amount
NUMERIC(18,2)

NOT NULL, CHECK > 0
currency
TEXT(3)

NOT NULL
status
TEXT

NOT NULL, DEFAULT DRAFT
provider_reference
TEXT

NULL
payment_date
DATE

NULL
idempotency_key
TEXT
UNIQUE
NOT NULL
created_by
UUID
FK
NOT NULL, REFERENCES user(user_id)
created_at
TIMESTAMPTZ

NOT NULL, DEFAULT now()


payment_event
Column
Type
Key
Constraints
event_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
instruction_id
UUID
FK
NOT NULL, REFERENCES payment_instruction(instruction_id)
event_type
TEXT

NOT NULL
provider_status
TEXT

NULL
payload
JSONB

NULL
occurred_at
TIMESTAMPTZ

NOT NULL, DEFAULT now()
recorded_by
UUID
FK
NULL, REFERENCES user(user_id)
complaint
Column
Type
Key
Constraints
complaint_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
beneficiary_id
UUID
FK
NULL, REFERENCES beneficiary(beneficiary_id)
instruction_id
UUID
FK
NULL, REFERENCES payment_instruction(instruction_id)
category
TEXT

NOT NULL
description
TEXT

NOT NULL
severity
TEXT

NOT NULL, DEFAULT MEDIUM
status
TEXT

NOT NULL, DEFAULT OPEN
assigned_to
UUID
FK
NULL, REFERENCES user(user_id)
opened_at
TIMESTAMPTZ

NOT NULL, DEFAULT now()
resolved_at
TIMESTAMPTZ

NULL
budget
Column
Type
Key
Constraints
budget_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
program_id
UUID
FK
NOT NULL, REFERENCES program(program_id)
name
TEXT

NOT NULL
total_amount
NUMERIC(18,2)

NOT NULL
currency
TEXT(3)

NOT NULL
status
TEXT

NOT NULL, DEFAULT DRAFT
created_by
UUID
FK
NOT NULL, REFERENCES user(user_id)
created_at
TIMESTAMPTZ

NOT NULL, DEFAULT now()
budget_line
Column
Type
Key
Constraints
budget_line_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
budget_id
UUID
FK
NOT NULL, REFERENCES budget(budget_id)
category
TEXT

NOT NULL
planned_amount
NUMERIC(18,2)

NOT NULL
actual_amount
NUMERIC(18,2)

NOT NULL, DEFAULT 0
notes
TEXT

NULL
audit_event
Column
Type
Key
Constraints
audit_id
UUID
PK
NOT NULL, DEFAULT uuid_generate_v4()
tenant_id
UUID
FK
NOT NULL, REFERENCES tenant(tenant_id)
actor_user_id
UUID
FK
NULL, REFERENCES user(user_id)
action
TEXT

NOT NULL
entity_type
TEXT

NOT NULL
entity_id
UUID

NULL
before_data
JSONB

NULL
after_data
JSONB

NULL
occurred_at
TIMESTAMPTZ

NOT NULL, DEFAULT now()


