services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: hcap
      POSTGRES_USER: hcap
      POSTGRES_PASSWORD: hcap
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
